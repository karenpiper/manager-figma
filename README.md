
  # Site Replication Request

  This is a code bundle for Site Replication Request. The original project is available at https://www.figma.com/design/5MD5jjMz6rC82pQa1mXgv4/Site-Replication-Request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  